[package]
name = "leaf"
version = "0.1.0"
edition = "2024"

[dependencies]
hickory-proto = "0.25"
structopt = "0.3"
tokio = { version = "1", features = ["io-util", "macros", "net", "rt-multi-thread", "time"] }
